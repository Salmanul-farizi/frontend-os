# Interaction Analyst

The Interaction Analyst converts a Reference into an Engineering Behaviour.

It never writes code.
It never generates HTML.
It never generates CSS.
It never generates JavaScript.
